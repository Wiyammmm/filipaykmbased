import 'package:flutter/material.dart';

class AppColors {
  static const Color secondaryColor = Color.fromARGB(255, 255, 225, 225);
  static const primaryColor = Color(0xFF00558d);

  // Add more colors as needed...
}
